# CodeGPT 项目结构说明

## 是否有「相似度检索」？

**有。** 对话时会把**当前用户文本**（可与附件合并后的内容）用 **Jina Embedding** 编成向量，再在 Supabase 里通过 RPC **`get_relevant_chunks`** 做 **向量相似度检索**（库内一般用余弦距离 `<=>`，按相似度排序取 Top-K），最后把检索结果拼进系统提示词，再走大模型流式回答。

| 环节 | 代码位置 |
|------|----------|
| 生成查询向量 | `app/api/chat/route.ts` 中的 `generateEmbedding()`（调用 `https://api.jina.ai/v1/embeddings`，默认模型 `jina-embeddings-v3`，维度由常量 `EMBEDDING_DIMENSION`，当前为 **1024**） |
| 相似度检索 | 同文件中的 `fetchRelevantContext()`：`supabase.rpc("get_relevant_chunks", { query_vector, match_threshold: 0.7, match_count: 3 })` |
| Prompt 注入 | 同文件中的 `createPrompt(context, userQuestion)` |

**知识库写入（建索引）**：`seed.ts` 按固定大小分块 → 同样用 Jina 生成向量 → 写入 Supabase `chunks` 表（与聊天侧使用同一套向量维度思路）。

> **注意**：仓库根目录的 `supabase_setup.sql` 示例里 `chunks.vector` 仍为 **1536** 维（偏旧说明）。若线上实际使用 Jina **1024** 维，请将数据库中的 `vector(...)` 维度与 `get_relevant_chunks` 的参数类型改为与 **`EMBEDDING_DIMENSION` / `seed.ts`** 一致，否则 RPC 会报错。以你当前 Supabase 里真实表结构为准。

---

## 目录结构（与当前仓库对齐）

```
CodeGPT/
├── app/
│   ├── layout.tsx, page.tsx, globals.css    # 根布局与首页（聊天主界面）
│   ├── login/, register/, person/, admin/   # 登录、注册、个人资料、管理页
│   └── api/
│       ├── chat/route.ts                    # 流式问答：检索 + DeepSeek
│       ├── upload/route.ts                  # 附件上传（需登录，POST）
│       ├── auth/login|logout|register/
│       ├── sessions/, sessions/[id]/, sessions/recent/
│       ├── person/profile/
│       └── admin/users/, admin/users/[id]/ # 管理端用户接口
├── components/
│   ├── ChatInput.tsx, ChatOutput.tsx
│   ├── UserDropdownMenu.tsx
│   ├── auth/AuthPageShell.tsx
│   ├── chat/UserAttachmentBubble.tsx
│   ├── home/ChatMainPanel.tsx, HomeSidebar.tsx, SessionFloatingMenu.tsx
│   └── ui/                                  # shadcn 风格基础组件
├── lib/
│   ├── supabase/server.ts, browser-client.ts, middleware.ts, env.ts
│   ├── server/supabase-admin.ts, ocr-image.ts
│   ├── chat/                                # 会话消息、附件解析等
│   ├── api/sessions-constants.ts, person-client.ts
│   ├── attachment-markdown.ts, upload-policy.ts
│   ├── auth-redirect.ts
│   ├── hooks/use-debounced-value.ts
│   ├── types/
│   └── utils.ts
├── supabase/migrations/                     # profiles、会话、附件、管理员等迁移
├── middleware.ts                            # Supabase 会话刷新等
├── seed.ts                                  # 知识库：分块、向量化、写入 chunks
├── supabase_setup.sql                       # 历史/示例：chunks + get_relevant_chunks（维度需与嵌入模型一致）
├── package.json, next.config.ts, tsconfig.json
├── .env.local                               # 本地环境变量（勿提交密钥）
└── 项目结构说明.md                          # 本文件
```

---

## 核心流程简述

### 1. 问答（带检索）

1. 前端提交消息（可含附件引用）→ `POST /api/chat`
2. `generateEmbedding()`：Jina 生成 **1024** 维查询向量  
3. `fetchRelevantContext()`：RPC **`get_relevant_chunks`**，`match_threshold` / `match_count` 可调  
4. `createPrompt()`：上下文 + 用户问题 → 系统提示  
5. `streamText`：经 **DeepSeek**（`DEEPSEEK_API_KEY`，`@ai-sdk/openai` 指向 DeepSeek 兼容端点）流式返回  

### 2. 附件上传

- `POST /api/upload`：登录校验后上传文件；策略与 OCR 等在 `lib/upload-policy.ts`、`lib/server/ocr-image.ts`。

### 3. 会话与账号

- 会话列表、重命名、删除等：`app/api/sessions/*` + `components/home/*`
- 个人资料：`app/person`、`app/api/person/profile`
- 管理端：`app/admin`、`app/api/admin/*`

### 4. 知识库构建

- 配置 `seed.ts` 中的路径或网页抓取相关选项后执行：`pnpm run seed`
- 分块逻辑在 `seed.ts` 内（按字符窗口 + overlap），**不再使用** 旧文档中的独立 `lib/code-processor.ts`。

---

## 环境变量（按代码实际使用）

在 `.env.local` 中按需配置（名称以项目代码为准）：

- **Supabase**：`SUPABASE_URL`、`SUPABASE_KEY`；服务端管理操作用 `lib/server/supabase-admin.ts` 中的变量（若已配置）
- **对话模型**：`DEEPSEEK_API_KEY`
- **向量**：`JINA_API_KEY`；可选 `JINA_EMBED_MODEL`（默认 `jina-embeddings-v3`）

---

## 依赖与脚本摘要

- **框架**：Next.js（App Router）、React 19、TypeScript、Tailwind 4  
- **AI**：`ai`、`@ai-sdk/openai`、`@ai-sdk/react`（对话端点指向 DeepSeek）  
- **数据**：`@supabase/supabase-js`、`@supabase/ssr`  
- **脚本**：`pnpm dev` / `pnpm build` / `pnpm start`，`pnpm run seed`  

---

## 开发建议（可选）

1. **检索**：调整 `fetchRelevantContext` 中的 `match_threshold`、`match_count` 以平衡覆盖面与噪声。  
2. **维度**：改嵌入模型或维度时，同步修改数据库 `chunks.vector` 与 `get_relevant_chunks` 的 `vector(...)` 类型。  
3. **限流**：注意 Jina、DeepSeek 的 API 频率与错误重试策略。  

---

## 扩展方向（与原项目设想一致）

- 多轮上下文与历史摘要  
- 独立「代码浏览 / 精确搜索」页面  
- 增量索引与重新向量化流水线  
- 可配置的系统提示模板与检索参数（环境变量或管理后台）
