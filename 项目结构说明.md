# CodeGPT 项目完整结构说明

## 📂 完整目录结构

```
CodeGPT/
│
├── 📁 app/                              # Next.js App Router 应用目录
│   ├── 📁 api/                          # API 路由目录
│   │   ├── 📁 chat/                     # 聊天相关 API
│   │   │   └── 📄 route.ts             # POST: 处理用户问题，检索上下文，生成回答
│   │   └── 📁 upload/                   # 代码上传相关 API
│   │       └── 📄 route.ts             # POST: 单文件上传 | PUT: 批量目录处理
│   │
│   ├── 📄 layout.tsx                    # 根布局组件（设置元数据和全局样式）
│   ├── 📄 page.tsx                      # 首页组件（聊天界面主页面）
│   ├── 📄 globals.css                   # 全局样式文件
│   └── 📄 favicon.ico                   # 网站图标
│
├── 📁 components/                       # React 组件目录
│   ├── 📁 ui/                           # 基础 UI 组件库（shadcn/ui）
│   │   ├── 📄 button.tsx                # 按钮组件
│   │   └── 📄 input.tsx                 # 输入框组件
│   │
│   ├── 📄 ChatInput.tsx                 # 聊天输入组件
│   │   └── 功能：用户输入框、提交按钮、输入处理
│   │
│   └── 📄 ChatOutput.tsx                # 聊天输出组件
│       └── 功能：消息列表渲染、Markdown 支持、代码高亮、状态显示
│
├── 📁 lib/                              # 工具库和业务逻辑
│   ├── 📄 code-processor.ts             # 代码处理核心模块 ⭐
│   │   ├── 支持的代码文件类型检测
│   │   ├── 编程语言识别
│   │   ├── 智能代码分块（CodeTextSplitter）
│   │   ├── 目录递归处理
│   │   └── 代码块元数据提取
│   │
│   └── 📄 utils.ts                      # 通用工具函数（如 cn 函数用于 className 合并）
│
├── 📁 public/                           # 静态资源目录
│   ├── 📄 next.svg                      # Next.js Logo
│   ├── 📄 vercel.svg                    # Vercel Logo
│   └── 📄 *.svg                         # 其他图标文件
│
├── 📄 seed.ts                           # 数据种子脚本 ⭐
│   └── 功能：批量导入代码文件/目录，构建向量索引
│
├── 📄 package.json                      # 项目依赖和脚本配置
├── 📄 package-lock.json                 # 依赖锁定文件
├── 📄 pnpm-lock.yaml                    # pnpm 锁定文件
│
├── 📄 tsconfig.json                     # TypeScript 配置文件
├── 📄 next.config.ts                    # Next.js 配置文件
├── 📄 components.json                   # UI 组件配置文件（shadcn/ui）
├── 📄 postcss.config.mjs                # PostCSS 配置文件
│
├── 📄 README.md                         # 项目说明文档（快速开始指南）
├── 📄 PROJECT_STRUCTURE.md              # 详细项目结构文档
├── 📄 项目结构说明.md                   # 本文件（中文结构说明）
│
├── 📄 supabase_setup.sql                # Supabase 数据库初始化 SQL 脚本
│   └── 包含：表结构、索引、函数定义
│
└── 📄 .env.local                        # 环境变量文件（需自行创建）
    └── 包含：Supabase 配置、OpenAI API 配置
```

## 🔑 核心文件说明

### 1. 代码处理模块 (`lib/code-processor.ts`)

**职责**：代码文件的解析、分块、元数据提取

**主要导出**：
- `CodeTextSplitter`: 智能代码分块器类
- `processCodeDirectory()`: 递归处理目录中的所有代码文件
- `isCodeFile()`: 判断文件是否为代码文件
- `getFileLanguage()`: 获取文件编程语言
- `readCodeFile()`: 读取代码文件内容
- `SUPPORTED_CODE_EXTENSIONS`: 支持的代码文件扩展名列表

**支持的编程语言**：
- Python (.py)
- JavaScript (.js, .jsx)
- TypeScript (.ts, .tsx)
- Java (.java)
- C/C++ (.c, .cpp, .h, .hpp)
- Go (.go)
- Rust (.rs)
- Ruby (.rb)
- PHP (.php)
- Swift (.swift)
- Kotlin (.kt)
- Scala (.scala)
- C# (.cs)
- 以及 Markdown, JSON, YAML 等文档格式

### 2. 聊天 API (`app/api/chat/route.ts`)

**职责**：处理用户问题，检索相关代码上下文，生成回答

**工作流程**：
1. 接收用户问题（POST 请求）
2. 生成问题向量（`generateEmbedding()`）
3. 从 Supabase 检索相关代码块（`fetchRelevantContext()`）
4. 构建包含上下文的 prompt（`createPrompt()`）
5. 调用 OpenAI API 生成回答（流式返回）

**关键函数**：
- `generateEmbedding()`: 使用 OpenAI embedding 模型生成向量
- `fetchRelevantContext()`: 调用 Supabase 函数检索相关代码块
- `createPrompt()`: 构建系统提示词，包含上下文和用户问题

### 3. 代码上传 API (`app/api/upload/route.ts`)

**职责**：处理代码文件上传，自动构建向量索引

**端点**：
- `POST /api/upload`: 上传单个代码文件
  - 请求：FormData，包含 file 和 filePath
  - 响应：处理结果（成功/失败，代码块数量）
  
- `PUT /api/upload`: 批量处理代码目录
  - 请求：JSON，包含 directoryPath
  - 响应：处理结果（总代码块数，成功处理数）

### 4. 数据种子脚本 (`seed.ts`)

**职责**：批量导入代码文件/目录，构建向量索引

**使用方式**：
1. 修改脚本中的 `codePaths` 和 `codeDocUrls` 配置
2. 运行 `npm run seed`
3. 脚本会自动处理所有代码文件并构建索引

**主要函数**：
- `loadFromCodeFiles()`: 从本地代码文件/目录加载
- `loadFromWebpages()`: 从网页加载代码文档
- `vectorizeAndStore()`: 向量化代码块并存储到 Supabase

### 5. 前端组件

#### ChatInput (`components/ChatInput.tsx`)
- 用户输入界面
- 提交处理
- 输入验证

#### ChatOutput (`components/ChatOutput.tsx`)
- 消息列表渲染（用户消息 + AI 回答）
- Markdown 渲染（使用 react-markdown）
- 代码语法高亮
- 状态显示（加载中、错误等）

### 6. 数据库结构 (`supabase_setup.sql`)

**chunks 表字段**：
- `id`: 主键
- `content`: 代码块内容
- `vector`: 向量嵌入（1536 维）
- `file_path`: 文件路径
- `url`: URL（兼容字段）
- `language`: 编程语言
- `metadata`: JSONB 元数据（行号、函数名等）
- `created_at`: 创建时间

**关键函数**：
- `get_relevant_chunks()`: 相似度搜索函数
- `get_chunks_stats()`: 统计函数

## 🔄 数据流程

### 索引构建流程

```
1. 代码文件/目录
   ↓
2. code-processor.ts
   - 识别代码文件
   - 智能分块（按函数/类）
   - 提取元数据
   ↓
3. OpenAI Embedding API
   - 生成 1536 维向量
   ↓
4. Supabase
   - 存储向量 + 元数据
   - 创建索引
```

### 问答流程

```
1. 用户输入问题
   ↓
2. 生成问题向量（Embedding）
   ↓
3. Supabase 向量检索
   - 计算余弦相似度
   - 返回 Top-K 相关代码块
   ↓
4. 构建 Prompt
   - 系统提示词
   - 检索到的上下文
   - 用户问题
   ↓
5. OpenAI GPT-4o-mini
   - 生成回答
   - 流式返回
   ↓
6. 前端渲染
   - Markdown 解析
   - 代码高亮
   - 显示来源
```

## 📦 依赖包说明

### 核心依赖

- `next`: Next.js 框架
- `react`, `react-dom`: React 库
- `@ai-sdk/openai`: OpenAI SDK
- `@ai-sdk/react`: React AI SDK hooks
- `ai`: Vercel AI SDK 核心
- `@supabase/supabase-js`: Supabase 客户端
- `langchain`: LangChain 框架
- `@langchain/community`: LangChain 社区扩展
- `react-markdown`: Markdown 渲染

### 开发依赖

- `typescript`: TypeScript 编译器
- `@types/node`, `@types/react`: 类型定义
- `tailwindcss`: CSS 框架
- `ts-node`: TypeScript 执行器（用于 seed.ts）

## 🚀 快速开始步骤

1. **安装依赖**
   ```bash
   npm install
   ```

2. **配置环境变量**
   - 复制 `.env.example` 为 `.env.local`
   - 填入 Supabase 和 OpenAI 配置

3. **初始化数据库**
   - 在 Supabase SQL Editor 中执行 `supabase_setup.sql`

4. **构建索引**
   - 修改 `seed.ts` 中的路径配置
   - 运行 `npm run seed`

5. **启动开发服务器**
   ```bash
   npm run dev
   ```

6. **访问应用**
   - 打开 http://localhost:3000

## 📝 开发建议

1. **代码分块大小**：建议 1000 字符，重叠 200 字符
2. **检索参数**：默认 Top-3，相似度阈值 0.7
3. **向量维度**：text-embedding-3-small 为 1536 维
4. **API 限流**：注意 OpenAI API 调用频率限制
5. **错误处理**：所有 API 都应包含错误处理逻辑

## 🔧 扩展建议

- 添加更多代码文件格式支持
- 实现代码执行和验证功能
- 支持多轮对话上下文
- 添加代码搜索和浏览界面
- 实现增量更新机制
- 添加用户认证和权限管理
- 支持自定义 prompt 模板
- 添加性能监控和评估指标
